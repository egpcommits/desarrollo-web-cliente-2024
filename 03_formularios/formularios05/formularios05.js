window.onload = () => {
    let formulario = document.forms[0];
    let comprobar = formulario["comprobar"];

    comprobar.addEventListener("click", () => {
        if (campo1 != '') {
            
        }
    }, false);
}